package util

const (
	NGX_DEFINE     = "#define"
	NGX_TYPEDEF    = "typedef"
	NGX_DEFINE_LEN = len(NGX_DEFINE)
	STRUCT         = "struct"
	UNION          = "union"
	UNION_TAG      = "/* union */"
)

const (
	PATH_CONFIG        = "conf/"
	NGINX_PARSE        = "nginx_parse.conf"
)

const (
	PATH_OUTPUT     = "output/"
	PATH_SOURCECODE = "source_code/"
	NGINX_ZIP       = "nginx.zip"
)

const (
	SHOW_PRINTLN = false
)
