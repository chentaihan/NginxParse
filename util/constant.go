package util

const (
	NGX_DEFINE			= "#define"
	NGX_DEFINE_LEN		= len(NGX_DEFINE)
)

const (
	PATH_CONFIG        = "conf/"
	NGINX_PARSE        = "nginx_parse.conf"
	FILE_CONFIG_FORMAT = "config_format.html"
)

const (
	PATH_OUTPUT = "output/"
)


const (
	SHOW_PRINTLN = false
)